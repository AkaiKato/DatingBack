namespace Domain.Enums
{
    public class DomainEnums
    {
        public enum Roles
        {
            Admin = 0,
            Moderator = 1,
            User = 2,
        }

        public enum Sex
        {
            Male = 0,
            Female = 1,
        }

        public enum MediaTypes
        {
            Photo = 0,
            Video = 1,
        }
    }
}
